### USE:

```
./go-tg-cli -msg="TEXT MSG" -file="/path/to/file" -image="/path/to/image"
# also you can:
# -mode="HTML||Markdown"
# -chat_id="123||or env"
# -topic_id="123||or env"
# -token="123||or env"
```
